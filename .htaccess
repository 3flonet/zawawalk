<IfModule mod_rewrite.c>
    RewriteEngine On

    # Paksa koneksi HTTP ke HTTPS
    RewriteCond %{HTTPS} off
    RewriteRule ^(.*)$ https://%{HTTP_HOST}%{REQUEST_URI} [L,R=301]

    # Arahkan semua permintaan ke folder public/
    RewriteRule ^(.*)$ public/$1 [L]
</IfModule>